{
  // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ng serve",
      "type": "chrome",
      "request": "launch",
      "preLaunchTask": "npm: start",
      "url": "http://localhost:4200/"
    }
    // "ng test" debug config removed: it pointed at Karma's debug.html, which
    // doesn't exist for this project's Vitest-based test runner. Debugging
    // Vitest in VS Code needs a different setup (Node debugger attach or the
    // official Vitest extension) - not re-added here since it wasn't verified.
  ]
}
